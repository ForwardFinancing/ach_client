version: 2.1
jobs:
  build:
    docker:
      - image: ruby:3.1.4
        environment:
          TZ: America/New_York
    steps:
      - checkout
      - run:
          name: Run the default task
          command: |
            gem install bundler -v 2.4.10
            bundle install
            bundle exec rake
